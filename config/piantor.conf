CONFIG_ZMK_KEYBOARD_NAME="Piantor"

# Enable mouse keys used by the base keymap
CONFIG_ZMK_POINTING=y
# CONFIG_ZMK_POINTING_SMOOTH_SCROLLING=y

# Wiring/split is handled by the Piantor shield; no BLE tweaks here
# (Leave BLE unset for wired build)
